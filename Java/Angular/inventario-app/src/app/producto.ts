export class Producto {
    idProducto?: number; //El simbolo "?" indica que el campo se inicializara luego
    descripcion?: string;
    precio?: number; //En TypeScript, los valores flotantes se manejan con "number"
    existencia?: number;
}
